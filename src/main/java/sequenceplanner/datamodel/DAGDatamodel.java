package sequenceplanner.datamodel;

/**
 * This class manages the product, operation and resource structure
 * in a DAG graph. 
 * 
 * DEV COMMENT. Maybe make this abstract since two versions can probably exists, one 
 * were only one operation structure exists and one with a prod. operation structure
 * and a resource op structure.
 * 
 * 
 * @author kbe
 */
public class DAGDatamodel {
    
}
